{
  "name": "globalpay-payment-manager-subgraph",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node ${GRAPH_DEPLOY_NODE:-https://api.thegraph.com/deploy/} ${GRAPH_SLUG:-globalpay/payment-manager}",
    "prepare": "graph codegen"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.35.1"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.31",
    "typescript": "^5.7.3"
  }
}
